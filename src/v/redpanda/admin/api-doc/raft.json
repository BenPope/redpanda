    "/raft/{group_id}/transfer_leadership": {
      "post": {
        "parameters": [
          {
            "in": "path",
            "name": "group_id",
            "required": true,
            "type": "integer"
          },
          {
            "in": "query",
            "name": "target",
            "type": "integer"
          }
        ],
        "responses": {
          "200": {
            "description": "Raft leadership transfer success",
            "schema": {}
          }
        },
        "tags": [
          "Partitions"
        ],
        "description": "Transfer leadership of the Raft group (`{group_id}`) to the node with `target` ID.",
        "operationId": "raft_transfer_leadership",
        "summary": "Transfer Raft group leadership"
      }
    },
    "/raft/recovery/status": {
      "get": {
        "parameters": [],
        "responses": {
          "200": {
            "description": "Raft recovery status",
            "schema": {
              "$ref": "#/definitions/recovery_status"
            }
          }
        },
        "description": "Get this node's recovery status",
        "operationId": "get_raft_recovery_status",
        "summary": "Get this node's recovery status"
      }
    }